# --- Dependencias principales del proyecto ---

# Librerías base para manejo numérico y de datos
numpy>=1.26.0
pandas>=2.2.1

# Utilidades del sistema y monitoreo
psutil>=5.9.5

# Progreso y utilidades varias
tqdm>=4.66.1


# Visualización de datos
matplotlib>=3.7.1

# Tokenización y manejo de texto 
tiktoken>=0.5.1


# PyTorch: requerido para la mayoría de los capítulos
torch>=2.2.2,<2.6; sys_platform == "darwin" and platform_machine == "x86_64"
torch>=2.2.2; sys_platform != "darwin" or platform_machine != "x86_64"


# TensorFlow: solo se requiere para cargar pesos de modelos preentrenados 
tensorflow>=2.16.2; sys_platform == "darwin" and platform_machine == "x86_64"
tensorflow>=2.18.0; sys_platform != "darwin" or platform_machine != "x86_64"
